

const NavBar = ({totalCounters}) => {
        return (
          <div>
          <a href="home">
          Navbar   <span>{totalCounters}</span>
        </a>
        </div>
    );

}
export default NavBar;
